namespace myMathNamespace {
  // here are a lot of functions to calc how to reach global domination
  export function calcWorld(arg: number) : number {
    return Math.floor(Math.random() * arg) + 42
  }
}

